package member.service;

import java.sql.Connection;
import java.sql.SQLException;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import jdbc.ConnectionProvider;
import member.dao.MemberDao;
import model.MemberListView;
import service.Service;

public class MemberListViewServiceImpl implements Service {
	
	
	
	private final int VIEW_MEMBER_PAGE_PER_COUNT = 3;
	
	@Override
	public String getViewPage(HttpServletRequest request, HttpServletResponse response) {
		MemberListView memberView = null;
		MemberDao dao = null;
		Connection conn = null;
		
		int startRow = Integer.parseInt(request.getAttribute("page"));
		int endRow = 0;
		int memberTotalCnt = dao.cntTotalMembers(conn);
		
		
		if (memberTotalCnt > 0) {
			startRow = ()
		}
		
		
		try {
			conn = ConnectionProvider.getConnection();
			dao.getMemberList(conn, endRow , VIEW_MEMBER_PAGE_PER_COUNT); // 리턴 받는거 처리할 방법 고민해야 함.
		} catch (SQLException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
		
		return "/WEB-INF/views/member/memberList.jsp";
	}

}
